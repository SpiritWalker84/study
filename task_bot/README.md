# Telegram-бот для командной работы с задачами

Telegram-бот для добавления задач в общий список с возможностью назначения ответственных и установки сроков выполнения.

## Возможности

- ➕ Добавление задач через пошаговый интерфейс
- 👤 Назначение ответственного за задачу
- 📅 Установка срока выполнения задачи
- 📋 Просмотр списка всех задач
- 📄 Экспорт задач в CSV-файл
- 🔘 Удобное меню с кнопками

## Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте файл `.env` на основе `.env-example`:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ```
4. Запустите бота:
   ```bash
   python main.py
   ```

## Использование

- `/start` - показать меню с кнопками
- `/add` - добавить новую задачу
- `/list` - показать все задачи
- `/list_csv` - экспортировать задачи в CSV

## Структура проекта

```
task_bot/
├── main.py              # Точка входа
├── requirements.txt     # Зависимости
├── .env-example        # Пример файла с переменными окружения
└── bot/                # Основной пакет бота
    ├── database/       # Работа с базой данных
    ├── handlers/       # Обработчики команд и сообщений
    └── keyboards/      # Клавиатуры для бота
```

## Технологии

- Python 3.8+
- aiogram 3.x
- SQLite3
- python-dotenv

## База данных

Используется SQLite3 с таблицей `tasks`:
- `id` - уникальный идентификатор
- `text` - текст задачи
- `user` - пользователь, создавший задачу
- `responsible` - ФИО ответственного (опционально)
- `deadline` - срок выполнения (опционально)
- `created_at` - дата и время создания
